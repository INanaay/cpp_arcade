* j'ai modifier le getUserEvent dans les lib graphiques pour qu'il renvoit un std::pair<userevent, char>
  ==> ca evite de faire ensuite un getChar (ce qui marche moyen en sfml pck si c'est 2 fonctions différents qui gèrent les event de clavier ca capote) du coup j'ai aussi rajouter un userEvent TEXT qui permet de savoir si l'user a renter un caractère alphanum qui sera stocké dans le .second du pair

* le menu se print bien en sfml mais il manques des baux (genre le scoreboard et le jeux choisi et tout), ya que le name qui se met bien pour le moment.

* j'ai décalé les structures MenuInformations et CoreInformations dans un fichier a part pck on doit les mettres dans IGlibs.hpp et du coup on inclus Core dans IGlibs alors que Core inclus IGlibs c'est relou je sais ya une technique pour palier a ca mais voila.

* j'ai juste changé les prototypes dans le coté ncurses pour que ca compile quand même mais j'ai pas trop testé.

* j'ai mis juste un MenuInformations a la main pour le moment dans la loop du DrawMenu du core histoire de bien set le name, apres faudra que je print le scoreboard et les jeux auquel on peut jouer, je sais pas si je dois printer aussi les lib graphiques disponibles et je réfléchis aussi a la maniere dont on va déterminer le jeux hilight (si on lui donne son nom ou si on passe l'index du jeu séléctionner dans le std::vector)
